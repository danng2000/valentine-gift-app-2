import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/valentine-gift-app/', // GitHub Pages repo name — change if repo name differs
})
